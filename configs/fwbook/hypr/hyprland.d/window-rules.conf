# =========================
# Window Rules
# =========================
# Rounding/clip is handled globally in general.conf (decoration.rounding = 12),
# mirroring the niri window-rule (geometry-corner-radius 12, clip-to-geometry).
# Below: float the small control/dialog utilities so they don't tile.

windowrulev2 = float, class:^(pavucontrol)$
windowrulev2 = float, class:^(blueman-manager)$
windowrulev2 = float, class:^(org.gnome.Calculator)$
windowrulev2 = float, title:^(Open File)$
windowrulev2 = float, title:^(Save File)$
windowrulev2 = float, class:^(xdg-desktop-portal-gtk)$
