-- =========================
-- Hyprland (hl-fws1)
-- =========================
-- Remote-managed slim desktop. Same tiling model + keybindings as fwbook,
-- but without a touchpad and without a locally-launched shell (DMS is the
-- shell; its config is generated elsewhere and mounted later).

require("conf/env")
require("conf/general")
require("conf/animations")
require("conf/monitors")
require("conf/inputs")
require("conf/window-rules")
require("conf/keybindings")
require("conf/autostart")
