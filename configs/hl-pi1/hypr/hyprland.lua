-- =========================
-- Hyprland (hl-pi1)
-- =========================
-- Small desktop clock box: a single Quickshell shell (configs/hl-pi1/quickshell/clock)
-- is auto-logged-in and fills the screen with a big clock + calendar.

require("conf/env")
require("conf/general")
require("conf/monitors")
require("conf/inputs")
require("conf/window-rules")
require("conf/keybindings")
require("conf/autostart")
