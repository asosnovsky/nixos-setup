-- =========================
-- Monitors
-- =========================
-- hl-fwdesk is a lounge console box on a single 1080p HDMI TV.
-- Fallback catches anything not matched above.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})
