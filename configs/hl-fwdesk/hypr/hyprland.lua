-- =========================
-- Hyprland (hl-fwdesk)
-- =========================
-- Steam-console desktop for the hl-fwdesk lounge box.
-- Config is baked into the build (skyg rootDir) and symlinked to
-- ~/.config/hypr by the skyg hyprland module.

require("conf/env")
require("conf/general")
require("conf/animations")
require("conf/monitors")
require("conf/inputs")
require("conf/window-rules")
require("conf/keybindings")
require("conf/autostart")
